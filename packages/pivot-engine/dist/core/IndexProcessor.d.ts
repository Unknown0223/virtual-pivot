import type { PivotConfig, PivotData } from "../types/pivot.types.js";
/** INDEX — (value × grand total) / (row total × column total). SUM asosida. */
export declare function applyIndexAggregations(data: PivotData, config: PivotConfig): PivotData;
//# sourceMappingURL=IndexProcessor.d.ts.map