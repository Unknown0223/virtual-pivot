{
  "slice": {
    "rows": [{ "uniqueName": "region_name" }],
    "columns": [],
    "measures": [
      { "uniqueName": "amount", "aggregation": "percentofrow" },
      { "uniqueName": "client_id", "aggregation": "distinctcount" }
    ],
    "reportFilters": [{ "uniqueName": "order_status" }]
  }
}
