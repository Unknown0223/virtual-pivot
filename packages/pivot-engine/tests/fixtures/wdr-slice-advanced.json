{
  "slice": {
    "rows": [{ "uniqueName": "region_name" }],
    "columns": [{ "uniqueName": "product_name" }],
    "measures": [
      { "uniqueName": "amount", "aggregation": "product" },
      { "uniqueName": "qty", "aggregation": "index" },
      { "uniqueName": "volume", "aggregation": "difference" },
      { "uniqueName": "amount", "aggregation": "runningtotals", "caption": "Yig'indiy summa" }
    ],
    "reportFilters": [
      {
        "uniqueName": "warehouse_name",
        "filter": { "members": ["Ombor A"], "exclude": false }
      }
    ]
  }
}
