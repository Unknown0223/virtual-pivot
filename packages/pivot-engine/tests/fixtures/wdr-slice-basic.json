{
  "slice": {
    "rows": [{ "uniqueName": "warehouse_name" }],
    "columns": [{ "uniqueName": "supervisor_code" }],
    "measures": [
      { "uniqueName": "amount", "aggregation": "sum" },
      { "uniqueName": "qty", "aggregation": "average" }
    ],
    "reportFilters": [
      {
        "uniqueName": "product_name",
        "filter": { "members": ["Mahsulot A", "Mahsulot B"] }
      }
    ]
  }
}
